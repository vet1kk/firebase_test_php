//create config.js file in the same directory with the following content:
//replace with your own app config from firebase
export const firebaseConfig = {
    apiKey: 'api-key',
    authDomain: 'project-id.firebaseapp.com',
    projectId: 'project-id',
    storageBucket: 'project-id.appspot.com',
    messagingSenderId: 'sender-id',
    appId: 'app-id',
    measurementId: 'G-measurement-id'
};

//replace with your own vapid key from firebase (key pair)
export const vapidKey = 'vapid-key';
